const nodeProject = {
  directories: ['config', 'controllers', 'data', 'models', 'routes', 'services', 'utils']
};

const reactProject = {
  directories: []
}

export { 
  nodeProject,
  reactProject
};